services:
  redis:
    image: redis
    ports:
      - 6379:6379
  
  celery:
    build: .
    command: celery -A run.celery worker --pool=prefork --concurrency=8 --loglevel=info
    volumes:
      - .:/app